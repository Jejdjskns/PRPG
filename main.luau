-- main.luau
-- Executor loader removed and replaced with a safe instructional placeholder.
--
-- If you want to execute the loader from an executor, copy and run one of the one-line commands below in your trusted executor environment.
-- IMPORTANT: Only run trusted code. Executing remote scripts is dangerous and can compromise accounts.
-- Ensure HTTP is enabled and your executor supports the used functions (game:HttpGet, HttpService:GetAsync, loadstring).

-- Example 1 — common executor / exploit style (game:HttpGet):
-- Copy and run exactly (replace URL if you host elsewhere):
-- loadstring(game:HttpGet("https://raw.githubusercontent.com/Jejdjskns/PRPG/main/main.luau"))()

-- Example 2 — using HttpService:GetAsync:
-- loadstring(game:GetService("HttpService"):GetAsync("https://raw.githubusercontent.com/Jejdjskns/PRPG/main/main.luau"))()

-- Notes:
-- - The original executable loader was removed to avoid accidental execution from the repository.
-- - If you need a local copy of the loader, fetch it from a trusted raw URL and run it locally in a safe environment.
-- - To restore an in-repo loader, replace this file with the loader code or create a separate loader file and document its usage here.
